import React from 'react';
import { Container as BootstrapContainer } from 'reactstrap';

import './Header.css';

const Footer = (props) => {
    return (
        <BootstrapContainer>
            <h2>Footer</h2>
        </BootstrapContainer>
    );
};

export default Footer;
